# i18n (Russian & Spanish) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Russian and Spanish language support to all pages and components using `next-intl` with cookie-based locale persistence (no URL changes).

**Architecture:** Install `next-intl`, configure `i18n/request.ts` to read locale from `NEXT_LOCALE` cookie (default `en`), wrap the app in `NextIntlClientProvider`, and add a `LanguageSwitcher` dropdown to the Navbar. Server components use `getTranslations()`, client components use `useTranslations()`.

**Tech Stack:** next-intl v3, Next.js 14 App Router, Clerk, TypeScript

---

## File Map

| Action | File |
|--------|------|
| Create | `i18n/request.ts` |
| Modify | `next.config.mjs` |
| Modify | `app/layout.tsx` |
| Create | `messages/en.json` |
| Create | `messages/ru.json` |
| Create | `messages/es.json` |
| Create | `app/api/set-locale/route.ts` |
| Create | `components/LanguageSwitcher.tsx` |
| Modify | `components/Navbar.tsx` |
| Modify | `components/Hero.tsx` |
| Modify | `components/SocialProofBar.tsx` |
| Modify | `components/Features.tsx` |
| Modify | `components/HowItWorks.tsx` |
| Modify | `components/PredictionTeaser.tsx` |
| Modify | `components/DishesPreview.tsx` |
| Modify | `components/PricingSection.tsx` |
| Modify | `components/CTASection.tsx` |
| Modify | `components/Footer.tsx` |
| Modify | `app/(auth)/forgot-password/page.tsx` |
| Modify | `components/dashboard/DashboardSidebar.tsx` |
| Modify | `components/dashboard/DashboardHeader.tsx` |
| Modify | `app/(dashboard)/overview/page.tsx` |
| Modify | `app/(main)/page.tsx` (remove Testimonials) |

---

## Task 1: Install next-intl and configure the request handler

**Files:**
- Create: `i18n/request.ts`
- Modify: `next.config.mjs`
- Modify: `app/layout.tsx`

- [ ] **Step 1: Install next-intl**

```bash
npm install next-intl
```

Expected output: `added 1 package` (or similar, no errors)

- [ ] **Step 2: Create `i18n/request.ts`**

```ts
import { getRequestConfig } from 'next-intl/server';
import { cookies } from 'next/headers';

const VALID_LOCALES = ['en', 'ru', 'es'] as const;

export default getRequestConfig(async () => {
  const cookieStore = cookies();
  const raw = cookieStore.get('NEXT_LOCALE')?.value ?? 'en';
  const locale = (VALID_LOCALES as readonly string[]).includes(raw) ? raw : 'en';

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 3: Update `next.config.mjs` to wrap with next-intl plugin**

Replace the entire file with:

```js
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'images.unsplash.com' },
      { protocol: 'https', hostname: '*.amazonaws.com' },
    ],
  },
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 4: Update `app/layout.tsx` to add `NextIntlClientProvider`**

Replace the file with:

```tsx
import type { Metadata } from "next";
import { ClerkProvider } from "@clerk/nextjs";
import { NextIntlClientProvider } from "next-intl";
import { getLocale, getMessages } from "next-intl/server";
import "./globals.css";

export const metadata: Metadata = {
  title: {
    default: "Wondish — Personalized Nutrition & Meal Planning",
    template: "%s | Wondish",
  },
  description:
    "Your AI-powered meal planning platform. Personalized recipes, nutrition tracking, and health dashboards tailored to your goals.",
  keywords: ["meal planning", "nutrition", "healthy eating", "recipes", "weight loss", "diet", "meal prep"],
  openGraph: {
    title: "Wondish — Personalized Nutrition & Meal Planning",
    description:
      "Your AI-powered meal planning platform. Personalized recipes, nutrition tracking, and health dashboards tailored to your goals.",
    type: "website",
    locale: "en_US",
  },
  twitter: {
    card: "summary_large_image",
    title: "Wondish — Personalized Nutrition & Meal Planning",
    description: "Your AI-powered meal planning platform tailored to your goals.",
  },
  robots: { index: true, follow: true },
};

export default async function RootLayout({ children }: { children: React.ReactNode }) {
  const locale = await getLocale();
  const messages = await getMessages();

  return (
    <ClerkProvider>
      <html lang={locale} suppressHydrationWarning>
        <body>
          <NextIntlClientProvider messages={messages}>
            {children}
          </NextIntlClientProvider>
        </body>
      </html>
    </ClerkProvider>
  );
}
```

- [ ] **Step 5: Verify build compiles**

```bash
npm run build
```

Expected: build succeeds with no type errors.

- [ ] **Step 6: Commit**

```bash
git add i18n/request.ts next.config.mjs app/layout.tsx package.json package-lock.json
git commit -m "feat: install next-intl and wire up provider"
```

---

## Task 2: Create complete message files

**Files:**
- Create: `messages/en.json`
- Create: `messages/ru.json`
- Create: `messages/es.json`

- [ ] **Step 1: Create `messages/en.json`**

```json
{
  "navbar": {
    "dishes": "Dishes",
    "pricing": "Pricing",
    "login": "Log in",
    "getStarted": "Get started",
    "dashboard": "Dashboard",
    "signOut": "Sign out"
  },
  "hero": {
    "badge": "Rated 4.9/5 by 10,000+ members",
    "headline1": "Every great meal",
    "headline2": "starts here.",
    "subheadline": "AI-powered meal plans built around your body, goals, and taste — scroll to see how it comes together.",
    "scroll": "scroll",
    "assembled": "assembled",
    "statsRecipes": "Recipes",
    "statsMembers": "Members",
    "statsPerMonth": "Per month",
    "getStarted": "Get started free",
    "browseDishes": "Browse dishes"
  },
  "socialProof": {
    "joinedBy": "Joined by",
    "members": "members",
    "rating": "from 2,400+ reviews",
    "recipes": "recipes",
    "freeTier": "Free tier available"
  },
  "features": {
    "eyebrow": "Everything you need",
    "headline": "Nutrition that works for your life",
    "subheadline": "Whether you're looking to lose weight, build muscle, or simply eat more mindfully — Wondish gives you the tools to succeed.",
    "premium": "Premium",
    "smartMealPlanningTitle": "Smart Meal Planning",
    "smartMealPlanningDesc": "Get a personalized weekly meal plan based on your caloric needs, dietary restrictions, and taste preferences. Updated automatically.",
    "healthGoalTrackingTitle": "Health Goal Tracking",
    "healthGoalTrackingDesc": "Track your weight, BMI, energy levels, and mood over time. Visualize your progress with beautiful charts and journey dashboards.",
    "customDishPreferencesTitle": "Custom Dish Preferences",
    "customDishPreferencesDesc": "Tell us what you love, what you avoid, and your allergies. We'll build a plan around your unique palate — no unwanted surprises.",
    "customIngredientsTitle": "Custom Ingredients",
    "customIngredientsDesc": "Add your own ingredients with exact nutritional values. Build recipes tailored to your kitchen and local grocery store.",
    "nutritionAnalyticsTitle": "Nutrition Analytics",
    "nutritionAnalyticsDesc": "Get detailed breakdowns of your macro and micronutrient intake. Understand the science behind your meals with clarity.",
    "weightLossPlansTitle": "Weight Loss Plans",
    "weightLossPlansDesc": "Curated low-calorie meal programs designed for healthy, sustainable weight loss — included in the free plan."
  },
  "predictionTeaser": {
    "eyebrow": "See your future",
    "headline": "See exactly how long it takes to reach your goal",
    "subheadline": "Enter your current weight, goal weight, and weekly target. Wondish tells you the exact number of days — and builds your meal plan to get there.",
    "getPrediction": "Get my prediction",
    "viewPlans": "View plans",
    "cardEyebrow": "Your Prediction",
    "days": "days",
    "cardSubtext": "to reach your goal weight — based on your profile",
    "current": "Current",
    "goal": "Goal",
    "weekly": "Weekly",
    "sampleText": "Sample prediction · Sign up to see yours"
  },
  "howItWorks": {
    "eyebrow": "Simple process",
    "headline": "Up and running in minutes",
    "step1Title": "Create your profile",
    "step1Desc": "Tell us about your health goals, dietary restrictions, allergies, and food preferences. Takes less than 3 minutes.",
    "step2Title": "Get your meal plan",
    "step2Desc": "Receive a personalized weekly plan with recipes tailored to your caloric needs and tastes — curated by nutritionists.",
    "step3Title": "Track your journey",
    "step3Desc": "Log meals, mood, weight, and energy. Watch your health improve week over week with our analytics dashboard."
  },
  "dishesPreview": {
    "eyebrow": "The menu",
    "headline": "Nutritious and delicious",
    "viewAll": "View all dishes"
  },
  "pricing": {
    "eyebrow": "Pricing",
    "headline": "Simple, honest pricing",
    "subheadline": "Start free. Upgrade when you're ready. No hidden fees.",
    "freeName": "Free",
    "freeTagline": "Start your nutrition journey at no cost.",
    "freeCta": "Get started free",
    "freeCtaLoggedIn": "Continue with Free →",
    "freeF1": "Weight loss meal templates",
    "freeF2": "BMI & calorie calculator",
    "freeF3": "Basic recipe access (50+ dishes)",
    "freeF4": "Daily nutrition overview",
    "freeF5": "Mood & weight journal",
    "freeF6": "Community recipes",
    "premiumName": "Premium",
    "premiumTagline": "The full Wondish experience.",
    "premiumCta": "Get Premium",
    "premiumCancelAnytime": "Cancel anytime.",
    "premiumF1": "Everything in Free",
    "premiumF2": "Full personalized meal planner",
    "premiumF3": "Custom dish preferences",
    "premiumF4": "Add custom ingredients",
    "premiumF5": "500+ curated recipes",
    "premiumF6": "Weekly & daily plan builder",
    "premiumF7": "Grocery & ingredient lists",
    "premiumF8": "Health journey dashboard",
    "premiumF9": "Advanced nutrition analytics",
    "premiumF10": "Priority support",
    "perMonth": "/ month",
    "footer": "No credit card required for the free plan · Cancel premium anytime · Secure billing via Stripe"
  },
  "cta": {
    "eyebrow": "Start today",
    "headline": "Ready to eat better, feel better?",
    "stat": "Average member loses 8.2 lbs in their first month.",
    "statHighlight": "8.2 lbs",
    "subheadline": "Join thousands of people building healthier habits with Wondish. Your first 14 days are completely free.",
    "getStarted": "Get started free",
    "browseMenu": "Browse the menu",
    "secureBilling": "Secure billing",
    "cancelAnytime": "Cancel anytime",
    "setupIn3min": "Setup in 3 min"
  },
  "footer": {
    "tagline": "Personalized nutrition and meal planning for a healthier life.",
    "product": "Product",
    "account": "Account",
    "legal": "Legal",
    "dishes": "Dishes",
    "pricing": "Pricing",
    "features": "Features",
    "login": "Log in",
    "getStarted": "Get started",
    "privacyPolicy": "Privacy Policy",
    "terms": "Terms of Service",
    "rights": "All rights reserved.",
    "securedBy": "Payments secured by"
  },
  "forgotPassword": {
    "title": "Forgot password?",
    "subtitle": "Enter your email and we'll send you a reset link.",
    "emailLabel": "Email",
    "emailPlaceholder": "you@example.com",
    "submit": "Send Reset Link",
    "rememberedIt": "Remembered it?",
    "signIn": "Sign in",
    "successTitle": "Check your inbox",
    "successSubtitle": "If an account exists for {email}, we've sent a password reset link.",
    "backToLogin": "← Back to login"
  },
  "sidebar": {
    "overview": "Overview",
    "mealPlan": "Meal Plan",
    "weeklyPlan": "Weekly Plan",
    "groceryList": "Grocery List",
    "myJournal": "My Journal",
    "myJourney": "My Journey",
    "myOrders": "My Orders",
    "myProfile": "My Profile",
    "myPrediction": "My Prediction",
    "myTaste": "My Taste",
    "myMembership": "My Membership",
    "admin": "Admin",
    "recipes": "Recipes",
    "users": "Users",
    "companies": "Companies",
    "parameters": "Parameters",
    "coupons": "Coupons",
    "signOut": "Sign out"
  },
  "dashboardHeader": {
    "myProfile": "My Profile",
    "redeemCoupon": "Redeem coupon",
    "redeemCouponTitle": "Redeem coupon",
    "signOut": "Sign out",
    "upgrade": "Free → Upgrade",
    "enterCoupon": "Enter a coupon code to unlock access.",
    "enterCode": "ENTER CODE",
    "apply": "Apply"
  },
  "overview": {
    "greeting": "Good to see you, {name} 👋",
    "snapshot": "Here's your health snapshot for today.",
    "todaysMeals": "Today's Meals",
    "mealsCompleted": "meals completed",
    "currentWeight": "Current Weight",
    "fromLastJournal": "from last journal entry",
    "streakLabel": "Streak",
    "streakDays": "{count} days",
    "consecutiveEntries": "consecutive journal entries",
    "todaysMealPlan": "Today's Meal Plan",
    "viewAll": "View all →",
    "noMealPlan": "No meal plan for today.",
    "setUpMealPlan": "Set up meal plan",
    "upgradeTitle": "Unlock the full experience",
    "upgradeDesc": "Upgrade to Premium for custom ingredients, full journey analytics, and priority support. First 14 days free.",
    "upgradeCta": "Upgrade to Premium — $15/mo"
  },
  "common": {
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete",
    "edit": "Edit",
    "loading": "Loading...",
    "error": "Something went wrong.",
    "success": "Success",
    "premium": "Premium",
    "free": "Free",
    "perMonth": "/ month"
  }
}
```

- [ ] **Step 2: Create `messages/ru.json`**

```json
{
  "navbar": {
    "dishes": "Блюда",
    "pricing": "Цены",
    "login": "Войти",
    "getStarted": "Начать",
    "dashboard": "Дашборд",
    "signOut": "Выйти"
  },
  "hero": {
    "badge": "Оценка 4.9/5 от более 10 000 участников",
    "headline1": "Каждый вкусный приём пищи",
    "headline2": "начинается здесь.",
    "subheadline": "Планы питания на основе ИИ, созданные с учётом вашего тела, целей и вкусов — прокрутите, чтобы увидеть, как это работает.",
    "scroll": "прокрутить",
    "assembled": "собрано",
    "statsRecipes": "Рецептов",
    "statsMembers": "Участников",
    "statsPerMonth": "В месяц",
    "getStarted": "Начать бесплатно",
    "browseDishes": "Смотреть блюда"
  },
  "socialProof": {
    "joinedBy": "Уже присоединились",
    "members": "участников",
    "rating": "из более 2 400 отзывов",
    "recipes": "рецептов",
    "freeTier": "Бесплатный тариф доступен"
  },
  "features": {
    "eyebrow": "Всё, что вам нужно",
    "headline": "Питание, которое работает для вашей жизни",
    "subheadline": "Хотите похудеть, набрать мышечную массу или просто питаться осознаннее — Wondish даст вам нужные инструменты.",
    "premium": "Премиум",
    "smartMealPlanningTitle": "Умное планирование питания",
    "smartMealPlanningDesc": "Получите персонализированный план питания на неделю с учётом ваших калорийных потребностей, диетических ограничений и вкусовых предпочтений. Обновляется автоматически.",
    "healthGoalTrackingTitle": "Отслеживание целей здоровья",
    "healthGoalTrackingDesc": "Отслеживайте вес, ИМТ, уровень энергии и настроение со временем. Визуализируйте прогресс с красивыми графиками и панелями.",
    "customDishPreferencesTitle": "Индивидуальные предпочтения блюд",
    "customDishPreferencesDesc": "Расскажите нам, что вы любите, что избегаете и на что у вас аллергия. Мы создадим план под ваш уникальный вкус — без неожиданностей.",
    "customIngredientsTitle": "Пользовательские ингредиенты",
    "customIngredientsDesc": "Добавляйте собственные ингредиенты с точными пищевыми ценностями. Создавайте рецепты под вашу кухню и местный магазин.",
    "nutritionAnalyticsTitle": "Аналитика питания",
    "nutritionAnalyticsDesc": "Получайте детальную разбивку потребления макро- и микронутриентов. Понимайте науку за вашими блюдами.",
    "weightLossPlansTitle": "Планы похудения",
    "weightLossPlansDesc": "Специально подобранные низкокалорийные программы питания для здорового и устойчивого снижения веса — включены в бесплатный план."
  },
  "predictionTeaser": {
    "eyebrow": "Увидьте своё будущее",
    "headline": "Узнайте, сколько именно времени нужно для достижения цели",
    "subheadline": "Введите текущий вес, целевой вес и еженедельную цель. Wondish скажет точное количество дней — и создаст план питания для достижения этой цели.",
    "getPrediction": "Получить прогноз",
    "viewPlans": "Смотреть планы",
    "cardEyebrow": "Ваш прогноз",
    "days": "дней",
    "cardSubtext": "до достижения целевого веса — на основе вашего профиля",
    "current": "Сейчас",
    "goal": "Цель",
    "weekly": "В неделю",
    "sampleText": "Образец прогноза · Зарегистрируйтесь, чтобы увидеть свой"
  },
  "howItWorks": {
    "eyebrow": "Простой процесс",
    "headline": "Готово к работе за несколько минут",
    "step1Title": "Создайте профиль",
    "step1Desc": "Расскажите о своих целях здоровья, диетических ограничениях, аллергиях и пищевых предпочтениях. Займёт менее 3 минут.",
    "step2Title": "Получите план питания",
    "step2Desc": "Получите персонализированный еженедельный план с рецептами, подобранными под ваши калорийные потребности и вкусы — составленный диетологами.",
    "step3Title": "Отслеживайте прогресс",
    "step3Desc": "Записывайте приёмы пищи, настроение, вес и энергию. Наблюдайте за улучшением здоровья неделя за неделей."
  },
  "dishesPreview": {
    "eyebrow": "Меню",
    "headline": "Полезно и вкусно",
    "viewAll": "Смотреть все блюда"
  },
  "pricing": {
    "eyebrow": "Цены",
    "headline": "Простые и честные цены",
    "subheadline": "Начните бесплатно. Обновитесь, когда будете готовы. Без скрытых платежей.",
    "freeName": "Бесплатно",
    "freeTagline": "Начните свой путь к правильному питанию бесплатно.",
    "freeCta": "Начать бесплатно",
    "freeCtaLoggedIn": "Продолжить бесплатно →",
    "freeF1": "Шаблоны питания для похудения",
    "freeF2": "Калькулятор ИМТ и калорий",
    "freeF3": "Базовый доступ к рецептам (50+ блюд)",
    "freeF4": "Ежедневный обзор питания",
    "freeF5": "Журнал настроения и веса",
    "freeF6": "Рецепты сообщества",
    "premiumName": "Премиум",
    "premiumTagline": "Полный опыт Wondish.",
    "premiumCta": "Получить Премиум",
    "premiumCancelAnytime": "Отмена в любое время.",
    "premiumF1": "Всё из Бесплатного",
    "premiumF2": "Полный персонализированный планировщик питания",
    "premiumF3": "Индивидуальные предпочтения блюд",
    "premiumF4": "Добавление пользовательских ингредиентов",
    "premiumF5": "500+ подобранных рецептов",
    "premiumF6": "Еженедельный и ежедневный конструктор плана",
    "premiumF7": "Списки продуктов и ингредиентов",
    "premiumF8": "Панель отслеживания здоровья",
    "premiumF9": "Расширенная аналитика питания",
    "premiumF10": "Приоритетная поддержка",
    "perMonth": "/ месяц",
    "footer": "Кредитная карта не требуется для бесплатного плана · Отмена Премиума в любое время · Безопасная оплата через Stripe"
  },
  "cta": {
    "eyebrow": "Начните сегодня",
    "headline": "Готовы питаться лучше и чувствовать себя лучше?",
    "stat": "Средний участник теряет 3,7 кг в первый месяц.",
    "statHighlight": "3,7 кг",
    "subheadline": "Присоединяйтесь к тысячам людей, формирующих более здоровые привычки с Wondish. Первые 14 дней полностью бесплатны.",
    "getStarted": "Начать бесплатно",
    "browseMenu": "Просмотреть меню",
    "secureBilling": "Безопасная оплата",
    "cancelAnytime": "Отмена в любое время",
    "setupIn3min": "Настройка за 3 мин"
  },
  "footer": {
    "tagline": "Персонализированное питание и планирование рациона для более здоровой жизни.",
    "product": "Продукт",
    "account": "Аккаунт",
    "legal": "Правовая информация",
    "dishes": "Блюда",
    "pricing": "Цены",
    "features": "Функции",
    "login": "Войти",
    "getStarted": "Начать",
    "privacyPolicy": "Политика конфиденциальности",
    "terms": "Условия использования",
    "rights": "Все права защищены.",
    "securedBy": "Платежи защищены"
  },
  "forgotPassword": {
    "title": "Забыли пароль?",
    "subtitle": "Введите вашу почту, и мы отправим вам ссылку для сброса пароля.",
    "emailLabel": "Эл. почта",
    "emailPlaceholder": "вы@example.com",
    "submit": "Отправить ссылку",
    "rememberedIt": "Вспомнили?",
    "signIn": "Войти",
    "successTitle": "Проверьте почту",
    "successSubtitle": "Если аккаунт с почтой {email} существует, мы отправили ссылку для сброса пароля.",
    "backToLogin": "← Назад ко входу"
  },
  "sidebar": {
    "overview": "Обзор",
    "mealPlan": "План питания",
    "weeklyPlan": "Недельный план",
    "groceryList": "Список продуктов",
    "myJournal": "Мой журнал",
    "myJourney": "Мой прогресс",
    "myOrders": "Мои заказы",
    "myProfile": "Мой профиль",
    "myPrediction": "Мой прогноз",
    "myTaste": "Мои вкусы",
    "myMembership": "Моя подписка",
    "admin": "Администрирование",
    "recipes": "Рецепты",
    "users": "Пользователи",
    "companies": "Компании",
    "parameters": "Параметры",
    "coupons": "Купоны",
    "signOut": "Выйти"
  },
  "dashboardHeader": {
    "myProfile": "Мой профиль",
    "redeemCoupon": "Активировать купон",
    "redeemCouponTitle": "Активировать купон",
    "signOut": "Выйти",
    "upgrade": "Бесплатно → Обновить",
    "enterCoupon": "Введите код купона для получения доступа.",
    "enterCode": "ВВЕДИТЕ КОД",
    "apply": "Применить"
  },
  "overview": {
    "greeting": "Рады видеть вас, {name} 👋",
    "snapshot": "Ваш снимок здоровья на сегодня.",
    "todaysMeals": "Сегодняшние блюда",
    "mealsCompleted": "приёмов пищи выполнено",
    "currentWeight": "Текущий вес",
    "fromLastJournal": "из последней записи журнала",
    "streakLabel": "Серия",
    "streakDays": "{count} дней",
    "consecutiveEntries": "последовательных записей в журнале",
    "todaysMealPlan": "Сегодняшний план питания",
    "viewAll": "Смотреть всё →",
    "noMealPlan": "Нет плана питания на сегодня.",
    "setUpMealPlan": "Настроить план питания",
    "upgradeTitle": "Откройте весь функционал",
    "upgradeDesc": "Обновитесь до Премиума для пользовательских ингредиентов, полной аналитики прогресса и приоритетной поддержки. Первые 14 дней бесплатно.",
    "upgradeCta": "Обновиться до Премиума — $15/мес"
  },
  "common": {
    "save": "Сохранить",
    "cancel": "Отмена",
    "delete": "Удалить",
    "edit": "Редактировать",
    "loading": "Загрузка...",
    "error": "Что-то пошло не так.",
    "success": "Успешно",
    "premium": "Премиум",
    "free": "Бесплатно",
    "perMonth": "/ месяц"
  }
}
```

- [ ] **Step 3: Create `messages/es.json`**

```json
{
  "navbar": {
    "dishes": "Platos",
    "pricing": "Precios",
    "login": "Iniciar sesión",
    "getStarted": "Comenzar",
    "dashboard": "Panel",
    "signOut": "Cerrar sesión"
  },
  "hero": {
    "badge": "Calificado 4.9/5 por más de 10.000 miembros",
    "headline1": "Cada gran comida",
    "headline2": "comienza aquí.",
    "subheadline": "Planes de alimentación con IA diseñados según tu cuerpo, metas y gustos — desplázate para ver cómo funciona.",
    "scroll": "desplazar",
    "assembled": "ensamblado",
    "statsRecipes": "Recetas",
    "statsMembers": "Miembros",
    "statsPerMonth": "Por mes",
    "getStarted": "Comenzar gratis",
    "browseDishes": "Ver platos"
  },
  "socialProof": {
    "joinedBy": "Unido por",
    "members": "miembros",
    "rating": "de más de 2.400 reseñas",
    "recipes": "recetas",
    "freeTier": "Plan gratuito disponible"
  },
  "features": {
    "eyebrow": "Todo lo que necesitas",
    "headline": "Nutrición que funciona para tu vida",
    "subheadline": "Ya sea que quieras perder peso, ganar músculo o simplemente comer de forma más consciente — Wondish te da las herramientas para lograrlo.",
    "premium": "Premium",
    "smartMealPlanningTitle": "Planificación inteligente de comidas",
    "smartMealPlanningDesc": "Obtén un plan de comidas semanal personalizado según tus necesidades calóricas, restricciones dietéticas y preferencias de sabor. Se actualiza automáticamente.",
    "healthGoalTrackingTitle": "Seguimiento de objetivos de salud",
    "healthGoalTrackingDesc": "Registra tu peso, IMC, niveles de energía y estado de ánimo con el tiempo. Visualiza tu progreso con gráficos y paneles de seguimiento.",
    "customDishPreferencesTitle": "Preferencias de platos personalizadas",
    "customDishPreferencesDesc": "Cuéntanos qué te encanta, qué evitas y tus alergias. Crearemos un plan adaptado a tu paladar único — sin sorpresas indeseadas.",
    "customIngredientsTitle": "Ingredientes personalizados",
    "customIngredientsDesc": "Agrega tus propios ingredientes con valores nutricionales exactos. Crea recetas adaptadas a tu cocina y tienda local.",
    "nutritionAnalyticsTitle": "Análisis nutricional",
    "nutritionAnalyticsDesc": "Obtén desgloses detallados de tu ingesta de macro y micronutrientes. Entiende la ciencia detrás de tus comidas con claridad.",
    "weightLossPlansTitle": "Planes de pérdida de peso",
    "weightLossPlansDesc": "Programas de comidas bajas en calorías diseñados para una pérdida de peso saludable y sostenible — incluidos en el plan gratuito."
  },
  "predictionTeaser": {
    "eyebrow": "Ve tu futuro",
    "headline": "Descubre exactamente cuánto tiempo necesitas para alcanzar tu meta",
    "subheadline": "Ingresa tu peso actual, peso objetivo y meta semanal. Wondish te dice el número exacto de días — y crea tu plan de comidas para lograrlo.",
    "getPrediction": "Obtener mi predicción",
    "viewPlans": "Ver planes",
    "cardEyebrow": "Tu Predicción",
    "days": "días",
    "cardSubtext": "para alcanzar tu peso objetivo — según tu perfil",
    "current": "Actual",
    "goal": "Meta",
    "weekly": "Semanal",
    "sampleText": "Predicción de muestra · Regístrate para ver la tuya"
  },
  "howItWorks": {
    "eyebrow": "Proceso simple",
    "headline": "Listo en minutos",
    "step1Title": "Crea tu perfil",
    "step1Desc": "Cuéntanos sobre tus objetivos de salud, restricciones dietéticas, alergias y preferencias alimentarias. Tarda menos de 3 minutos.",
    "step2Title": "Obtén tu plan de comidas",
    "step2Desc": "Recibe un plan semanal personalizado con recetas adaptadas a tus necesidades calóricas y gustos — elaborado por nutricionistas.",
    "step3Title": "Registra tu progreso",
    "step3Desc": "Registra comidas, estado de ánimo, peso y energía. Observa cómo mejora tu salud semana a semana con nuestro panel de análisis."
  },
  "dishesPreview": {
    "eyebrow": "El menú",
    "headline": "Nutritivo y delicioso",
    "viewAll": "Ver todos los platos"
  },
  "pricing": {
    "eyebrow": "Precios",
    "headline": "Precios simples y honestos",
    "subheadline": "Empieza gratis. Mejora cuando estés listo. Sin tarifas ocultas.",
    "freeName": "Gratis",
    "freeTagline": "Comienza tu camino nutricional sin costo.",
    "freeCta": "Comenzar gratis",
    "freeCtaLoggedIn": "Continuar con Gratis →",
    "freeF1": "Plantillas de comidas para pérdida de peso",
    "freeF2": "Calculadora de IMC y calorías",
    "freeF3": "Acceso básico a recetas (50+ platos)",
    "freeF4": "Resumen nutricional diario",
    "freeF5": "Diario de estado de ánimo y peso",
    "freeF6": "Recetas de la comunidad",
    "premiumName": "Premium",
    "premiumTagline": "La experiencia completa de Wondish.",
    "premiumCta": "Obtener Premium",
    "premiumCancelAnytime": "Cancela cuando quieras.",
    "premiumF1": "Todo en Gratis",
    "premiumF2": "Planificador de comidas personalizado completo",
    "premiumF3": "Preferencias de platos personalizadas",
    "premiumF4": "Agregar ingredientes personalizados",
    "premiumF5": "500+ recetas seleccionadas",
    "premiumF6": "Constructor de planes semanal y diario",
    "premiumF7": "Listas de compras e ingredientes",
    "premiumF8": "Panel de seguimiento de salud",
    "premiumF9": "Análisis nutricional avanzado",
    "premiumF10": "Soporte prioritario",
    "perMonth": "/ mes",
    "footer": "No se requiere tarjeta de crédito para el plan gratuito · Cancela Premium cuando quieras · Facturación segura vía Stripe"
  },
  "cta": {
    "eyebrow": "Empieza hoy",
    "headline": "¿Listo para comer mejor, sentirte mejor?",
    "stat": "El miembro promedio pierde 3,7 kg en su primer mes.",
    "statHighlight": "3,7 kg",
    "subheadline": "Únete a miles de personas construyendo hábitos más saludables con Wondish. Tus primeros 14 días son completamente gratis.",
    "getStarted": "Comenzar gratis",
    "browseMenu": "Ver el menú",
    "secureBilling": "Facturación segura",
    "cancelAnytime": "Cancela cuando quieras",
    "setupIn3min": "Configuración en 3 min"
  },
  "footer": {
    "tagline": "Nutrición personalizada y planificación de comidas para una vida más saludable.",
    "product": "Producto",
    "account": "Cuenta",
    "legal": "Legal",
    "dishes": "Platos",
    "pricing": "Precios",
    "features": "Características",
    "login": "Iniciar sesión",
    "getStarted": "Comenzar",
    "privacyPolicy": "Política de privacidad",
    "terms": "Términos de servicio",
    "rights": "Todos los derechos reservados.",
    "securedBy": "Pagos asegurados por"
  },
  "forgotPassword": {
    "title": "¿Olvidaste tu contraseña?",
    "subtitle": "Ingresa tu correo y te enviaremos un enlace para restablecer tu contraseña.",
    "emailLabel": "Correo electrónico",
    "emailPlaceholder": "tu@ejemplo.com",
    "submit": "Enviar enlace",
    "rememberedIt": "¿La recordaste?",
    "signIn": "Iniciar sesión",
    "successTitle": "Revisa tu bandeja de entrada",
    "successSubtitle": "Si existe una cuenta para {email}, te hemos enviado un enlace para restablecer tu contraseña.",
    "backToLogin": "← Volver al inicio de sesión"
  },
  "sidebar": {
    "overview": "Resumen",
    "mealPlan": "Plan de comidas",
    "weeklyPlan": "Plan semanal",
    "groceryList": "Lista de compras",
    "myJournal": "Mi diario",
    "myJourney": "Mi progreso",
    "myOrders": "Mis pedidos",
    "myProfile": "Mi perfil",
    "myPrediction": "Mi predicción",
    "myTaste": "Mis gustos",
    "myMembership": "Mi membresía",
    "admin": "Administración",
    "recipes": "Recetas",
    "users": "Usuarios",
    "companies": "Empresas",
    "parameters": "Parámetros",
    "coupons": "Cupones",
    "signOut": "Cerrar sesión"
  },
  "dashboardHeader": {
    "myProfile": "Mi perfil",
    "redeemCoupon": "Canjear cupón",
    "redeemCouponTitle": "Canjear cupón",
    "signOut": "Cerrar sesión",
    "upgrade": "Gratis → Mejorar",
    "enterCoupon": "Ingresa un código de cupón para desbloquear el acceso.",
    "enterCode": "INGRESA CÓDIGO",
    "apply": "Aplicar"
  },
  "overview": {
    "greeting": "Qué bueno verte, {name} 👋",
    "snapshot": "Aquí está tu resumen de salud para hoy.",
    "todaysMeals": "Comidas de hoy",
    "mealsCompleted": "comidas completadas",
    "currentWeight": "Peso actual",
    "fromLastJournal": "del último registro del diario",
    "streakLabel": "Racha",
    "streakDays": "{count} días",
    "consecutiveEntries": "registros consecutivos en el diario",
    "todaysMealPlan": "Plan de comidas de hoy",
    "viewAll": "Ver todo →",
    "noMealPlan": "No hay plan de comidas para hoy.",
    "setUpMealPlan": "Configurar plan de comidas",
    "upgradeTitle": "Desbloquea la experiencia completa",
    "upgradeDesc": "Mejora a Premium para ingredientes personalizados, análisis completo del progreso y soporte prioritario. Los primeros 14 días son gratis.",
    "upgradeCta": "Mejorar a Premium — $15/mes"
  },
  "common": {
    "save": "Guardar",
    "cancel": "Cancelar",
    "delete": "Eliminar",
    "edit": "Editar",
    "loading": "Cargando...",
    "error": "Algo salió mal.",
    "success": "Éxito",
    "premium": "Premium",
    "free": "Gratis",
    "perMonth": "/ mes"
  }
}
```

- [ ] **Step 4: Verify build still passes**

```bash
npm run build
```

Expected: build succeeds.

- [ ] **Step 5: Commit**

```bash
git add messages/
git commit -m "feat: add en/ru/es message files"
```

---

## Task 3: Create the set-locale API route

**Files:**
- Create: `app/api/set-locale/route.ts`

- [ ] **Step 1: Create `app/api/set-locale/route.ts`**

```ts
import { NextRequest, NextResponse } from "next/server";

const VALID_LOCALES = ["en", "ru", "es"];

export async function POST(req: NextRequest) {
  const { locale } = await req.json();
  if (!VALID_LOCALES.includes(locale)) {
    return NextResponse.json({ error: "Invalid locale" }, { status: 400 });
  }
  const res = NextResponse.json({ ok: true });
  res.cookies.set("NEXT_LOCALE", locale, {
    maxAge: 60 * 60 * 24 * 365,
    path: "/",
    sameSite: "lax",
  });
  return res;
}
```

- [ ] **Step 2: Commit**

```bash
git add app/api/set-locale/route.ts
git commit -m "feat: add set-locale API route"
```

---

## Task 4: Create LanguageSwitcher and translate Navbar

**Files:**
- Create: `components/LanguageSwitcher.tsx`
- Modify: `components/Navbar.tsx`

- [ ] **Step 1: Create `components/LanguageSwitcher.tsx`**

```tsx
"use client";

import { useLocale } from "next-intl";
import { useRouter } from "next/navigation";
import { useState } from "react";

const LOCALES = [
  { code: "en", flag: "🇬🇧", label: "EN" },
  { code: "ru", flag: "🇷🇺", label: "RU" },
  { code: "es", flag: "🇪🇸", label: "ES" },
];

export default function LanguageSwitcher() {
  const locale = useLocale();
  const router = useRouter();
  const [open, setOpen] = useState(false);
  const current = LOCALES.find((l) => l.code === locale) ?? LOCALES[0];

  async function handleSelect(code: string) {
    await fetch("/api/set-locale", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ locale: code }),
    });
    setOpen(false);
    router.refresh();
  }

  return (
    <div className="relative">
      <button
        onClick={() => setOpen((v) => !v)}
        className="flex items-center gap-1.5 text-white/60 hover:text-white px-3 py-2 rounded-lg hover:bg-white/[0.06] transition-all duration-150 text-sm font-medium"
      >
        <span>{current.flag}</span>
        <span>{current.label}</span>
        <svg width="10" height="6" viewBox="0 0 10 6" fill="none">
          <path d="M1 1l4 4 4-4" stroke="currentColor" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round"/>
        </svg>
      </button>

      {open && (
        <div className="absolute right-0 top-11 w-28 bg-forest-deeper border border-white/10 rounded-xl shadow-xl overflow-hidden z-50">
          {LOCALES.map((l) => (
            <button
              key={l.code}
              onClick={() => handleSelect(l.code)}
              className={`w-full flex items-center gap-2.5 px-3 py-2.5 text-sm transition-colors ${
                l.code === locale
                  ? "text-primary bg-primary/10"
                  : "text-white/60 hover:text-white hover:bg-white/[0.06]"
              }`}
            >
              <span>{l.flag}</span>
              <span className="font-medium">{l.label}</span>
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Replace `components/Navbar.tsx` with translated version**

```tsx
"use client";

import Link from "next/link";
import { useState } from "react";
import { useAuth, useClerk } from "@clerk/nextjs";
import { useTranslations } from "next-intl";
import LanguageSwitcher from "./LanguageSwitcher";

export default function Navbar() {
  const [open, setOpen] = useState(false);
  const { isSignedIn } = useAuth();
  const { signOut } = useClerk();
  const t = useTranslations("navbar");

  return (
    <header className="fixed top-0 left-0 right-0 z-50 bg-forest-deeper/95 backdrop-blur-md border-b border-white/[0.07]">
      <div className="max-w-6xl mx-auto px-5 sm:px-8">
        <div className="flex items-center justify-between h-16">

          {/* Logo */}
          <Link href="/" className="flex items-center gap-2.5 group">
            <div className="w-8 h-8 bg-primary rounded-lg flex items-center justify-center shadow-lg shadow-primary/20">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                <path
                  d="M8 1.5C4.41 1.5 1.5 4.41 1.5 8S4.41 14.5 8 14.5 14.5 11.59 14.5 8 11.59 1.5 8 1.5ZM8 5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3Zm0 7.5a5.25 5.25 0 0 1-4.5-2.55c.02-1.5 3-2.325 4.5-2.325s4.48.825 4.5 2.325A5.25 5.25 0 0 1 8 12.5Z"
                  fill="white"
                />
              </svg>
            </div>
            <span className="text-white font-semibold text-lg tracking-tight">Wondish</span>
          </Link>

          {/* Center nav */}
          <nav className="hidden md:flex items-center gap-1">
            <Link href="/dishes" className="text-white/60 hover:text-white px-4 py-2 rounded-lg hover:bg-white/[0.06] transition-all duration-150 text-sm font-medium">
              {t("dishes")}
            </Link>
            <Link href="/pricing" className="text-white/60 hover:text-white px-4 py-2 rounded-lg hover:bg-white/[0.06] transition-all duration-150 text-sm font-medium">
              {t("pricing")}
            </Link>
          </nav>

          {/* Right actions */}
          <div className="hidden md:flex items-center gap-2">
            <LanguageSwitcher />
            {isSignedIn ? (
              <>
                <Link href="/overview" className="bg-primary hover:bg-primary-dark text-forest-deeper px-4 py-2 rounded-lg transition-all duration-150 text-sm font-semibold shadow-lg shadow-primary/20">
                  {t("dashboard")}
                </Link>
                <button
                  onClick={() => signOut({ redirectUrl: "/" })}
                  className="text-white/60 hover:text-white px-4 py-2 rounded-lg hover:bg-white/[0.06] transition-all duration-150 text-sm font-medium"
                >
                  {t("signOut")}
                </button>
              </>
            ) : (
              <>
                <Link href="/login" className="text-white/60 hover:text-white px-4 py-2 rounded-lg hover:bg-white/[0.06] transition-all duration-150 text-sm font-medium">
                  {t("login")}
                </Link>
                <Link href="/register" className="bg-primary hover:bg-primary-dark text-forest-deeper px-4 py-2 rounded-lg transition-all duration-150 text-sm font-semibold shadow-lg shadow-primary/20">
                  {t("getStarted")}
                </Link>
              </>
            )}
          </div>

          {/* Mobile toggle */}
          <button onClick={() => setOpen(!open)} className="md:hidden p-2 text-white/60 hover:text-white" aria-label="Toggle menu">
            {open ? (
              <svg width="20" height="20" viewBox="0 0 20 20" fill="currentColor">
                <path fillRule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clipRule="evenodd" />
              </svg>
            ) : (
              <svg width="20" height="20" viewBox="0 0 20 20" fill="currentColor">
                <path fillRule="evenodd" d="M3 5a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 10a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 15a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z" clipRule="evenodd" />
              </svg>
            )}
          </button>
        </div>
      </div>

      {/* Mobile menu */}
      {open && (
        <div className="md:hidden border-t border-white/[0.06] bg-forest-deeper">
          <div className="max-w-6xl mx-auto px-5 py-4 flex flex-col gap-1">
            <Link href="/dishes" onClick={() => setOpen(false)} className="text-white/60 hover:text-white px-3 py-2.5 rounded-lg hover:bg-white/[0.06] text-sm font-medium">
              {t("dishes")}
            </Link>
            <Link href="/pricing" onClick={() => setOpen(false)} className="text-white/60 hover:text-white px-3 py-2.5 rounded-lg hover:bg-white/[0.06] text-sm font-medium">
              {t("pricing")}
            </Link>
            <div className="border-t border-white/[0.06] mt-2 pt-2 flex flex-col gap-2">
              {isSignedIn ? (
                <>
                  <Link href="/overview" onClick={() => setOpen(false)} className="bg-primary text-forest-deeper px-3 py-2.5 rounded-lg text-sm font-semibold text-center">
                    {t("dashboard")}
                  </Link>
                  <button
                    onClick={() => signOut({ redirectUrl: "/" })}
                    className="text-white/60 hover:text-white px-3 py-2.5 rounded-lg hover:bg-white/[0.06] text-sm font-medium text-left"
                  >
                    {t("signOut")}
                  </button>
                </>
              ) : (
                <>
                  <Link href="/login" onClick={() => setOpen(false)} className="text-white/60 hover:text-white px-3 py-2.5 rounded-lg hover:bg-white/[0.06] text-sm font-medium">
                    {t("login")}
                  </Link>
                  <Link href="/register" onClick={() => setOpen(false)} className="bg-primary text-forest-deeper px-3 py-2.5 rounded-lg text-sm font-semibold text-center">
                    {t("getStarted")}
                  </Link>
                </>
              )}
              <div className="pt-1">
                <LanguageSwitcher />
              </div>
            </div>
          </div>
        </div>
      )}
    </header>
  );
}
```

- [ ] **Step 3: Verify build**

```bash
npm run build
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add components/LanguageSwitcher.tsx components/Navbar.tsx
git commit -m "feat: add LanguageSwitcher and translate Navbar"
```

---

## Task 5: Translate Hero

**Files:**
- Modify: `components/Hero.tsx`

- [ ] **Step 1: Replace the hardcoded strings in `components/Hero.tsx`**

`Hero` is a client component. Add `useTranslations` at the top of the component function and replace every hardcoded string.

Replace these specific lines:

```tsx
// At the top of the file, add:
import { useTranslations } from "next-intl";
```

Inside the `Hero` component function, add as the first line:
```tsx
const t = useTranslations("hero");
```

Then replace hardcoded strings one by one:

| Old | New |
|-----|-----|
| `"Rated 4.9/5 by 10,000+ members"` | `{t("badge")}` |
| `Every great meal` | `{t("headline1")}` |
| `<span className="text-[#4ade80]">starts here.</span>` | `<span className="text-[#4ade80]">{t("headline2")}</span>` |
| `AI-powered meal plans built around your body...` | `{t("subheadline")}` |
| `<span className="text-xs text-white/20 tracking-widest uppercase">scroll</span>` | `<span className="text-xs text-white/20 tracking-widest uppercase">{t("scroll")}</span>` |
| `{ value: "500+", label: "Recipes" }` | `{ value: "500+", label: t("statsRecipes") }` |
| `{ value: "10k+", label: "Members" }` | `{ value: "10k+", label: t("statsMembers") }` |
| `{ value: "$15", label: "Per month" }` | `{ value: "$15", label: t("statsPerMonth") }` |
| `Get started free` (Link text) | `{t("getStarted")}` |
| `Browse dishes` (Link text) | `{t("browseDishes")}` |
| `assembled` (motion.div text) | `{t("assembled")}` |

- [ ] **Step 2: Verify build**

```bash
npm run build
```

- [ ] **Step 3: Commit**

```bash
git add components/Hero.tsx
git commit -m "feat: translate Hero component"
```

---

## Task 6: Translate SocialProofBar, Features, HowItWorks

**Files:**
- Modify: `components/SocialProofBar.tsx`
- Modify: `components/Features.tsx`
- Modify: `components/HowItWorks.tsx`

- [ ] **Step 1: Update `components/SocialProofBar.tsx`**

Add `import { getTranslations } from "next-intl/server";` and make the component async:

```tsx
import { getTranslations } from "next-intl/server";

const avatars = ["A", "M", "S", "J", "R"];
const avatarColors = ["bg-emerald-500", "bg-violet-500", "bg-sky-500", "bg-orange-500", "bg-pink-500"];

export default async function SocialProofBar() {
  const t = await getTranslations("socialProof");

  return (
    <div className="bg-[#0d1a10] border-b border-white/5 py-4 px-5 sm:px-8">
      <div className="max-w-6xl mx-auto flex flex-col sm:flex-row items-center justify-center gap-6 sm:gap-10">
        <div className="flex items-center gap-3">
          <div className="flex -space-x-2">
            {avatars.map((letter, i) => (
              <div key={i} className={`w-7 h-7 rounded-full ${avatarColors[i]} border-2 border-[#0d1a10] flex items-center justify-center text-white text-[10px] font-bold`}>
                {letter}
              </div>
            ))}
          </div>
          <span className="text-white/50 text-sm">
            {t("joinedBy")} <span className="text-white font-semibold">10,000+</span> {t("members")}
          </span>
        </div>
        <div className="hidden sm:block w-px h-5 bg-white/10" />
        <div className="flex items-center gap-2">
          <div className="flex text-yellow-400 text-sm">{"★★★★★"}</div>
          <span className="text-white/50 text-sm">
            <span className="text-white font-semibold">4.9/5</span> {t("rating")}
          </span>
        </div>
        <div className="hidden sm:block w-px h-5 bg-white/10" />
        <div className="flex items-center gap-5 text-sm text-white/40">
          <span><span className="text-white/70 font-medium">500+</span> {t("recipes")}</span>
          <span>·</span>
          <span><span className="text-white/70 font-medium">{t("freeTier")}</span></span>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Update `components/Features.tsx`**

Make async, move the features array inside the component, and use `t()` for strings. Keep icons as JSX:

```tsx
import { getTranslations } from "next-intl/server";

export default async function Features() {
  const t = await getTranslations("features");

  const features = [
    {
      icon: (
        <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
          <rect x="2" y="3" width="18" height="16" rx="2" stroke="currentColor" strokeWidth="1.5" />
          <path d="M7 3V7M15 3V7M2 9H20" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
          <path d="M7 13H7.01M11 13H11.01M15 13H15.01M7 16H7.01M11 16H11.01" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
        </svg>
      ),
      title: t("smartMealPlanningTitle"),
      description: t("smartMealPlanningDesc"),
      premium: true,
    },
    {
      icon: (
        <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
          <path d="M11 2C8.24 2 6 4.24 6 7c0 3.53 4 8 5 9 1-1 5-5.47 5-9 0-2.76-2.24-5-5-5Z" stroke="currentColor" strokeWidth="1.5" />
          <circle cx="11" cy="7" r="2" stroke="currentColor" strokeWidth="1.5" />
          <path d="M3 19c2-2 5-3 8-3s6 1 8 3" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
        </svg>
      ),
      title: t("healthGoalTrackingTitle"),
      description: t("healthGoalTrackingDesc"),
      premium: false,
    },
    {
      icon: (
        <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
          <circle cx="11" cy="11" r="9" stroke="currentColor" strokeWidth="1.5" />
          <path d="M11 7v4l3 3" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
        </svg>
      ),
      title: t("customDishPreferencesTitle"),
      description: t("customDishPreferencesDesc"),
      premium: true,
    },
    {
      icon: (
        <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
          <path d="M3 6h4l2 5h6l2-5h4" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
          <circle cx="8" cy="16" r="1.5" stroke="currentColor" strokeWidth="1.5" />
          <circle cx="16" cy="16" r="1.5" stroke="currentColor" strokeWidth="1.5" />
        </svg>
      ),
      title: t("customIngredientsTitle"),
      description: t("customIngredientsDesc"),
      premium: true,
    },
    {
      icon: (
        <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
          <path d="M4 17L8 9l4 5 3-3 3 6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
          <rect x="2" y="2" width="18" height="18" rx="2" stroke="currentColor" strokeWidth="1.5" />
        </svg>
      ),
      title: t("nutritionAnalyticsTitle"),
      description: t("nutritionAnalyticsDesc"),
      premium: false,
    },
    {
      icon: (
        <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
          <path d="M11 2l2.09 6.26H20l-5.46 3.97 2.09 6.26L11 14.52l-5.63 3.97 2.09-6.26L2 8.26h6.91z" stroke="currentColor" strokeWidth="1.5" strokeLinejoin="round" />
        </svg>
      ),
      title: t("weightLossPlansTitle"),
      description: t("weightLossPlansDesc"),
      premium: false,
    },
  ];

  const [featured, ...rest] = features;

  return (
    <section className="bg-white py-24 px-5 sm:px-8">
      <div className="max-w-6xl mx-auto">
        <div className="max-w-2xl mb-16">
          <p className="text-primary text-sm font-semibold uppercase tracking-widest mb-4">{t("eyebrow")}</p>
          <h2 className="text-4xl sm:text-5xl font-bold text-[#25293C] leading-tight mb-4">{t("headline")}</h2>
          <p className="text-[#8A8D93] text-lg leading-relaxed">{t("subheadline")}</p>
        </div>

        <div className="relative bg-gradient-to-br from-primary/10 via-primary/5 to-transparent border border-primary/20 rounded-2xl p-8 sm:p-10 mb-6 overflow-hidden">
          <div className="absolute -top-16 -right-16 w-64 h-64 rounded-full bg-primary/8 pointer-events-none" />
          <div className="absolute bottom-0 left-0 w-48 h-48 rounded-full bg-primary/5 blur-[60px] pointer-events-none" />
          <div className="relative max-w-2xl">
            {featured.premium && (
              <span className="inline-block text-[10px] font-semibold text-primary bg-primary/10 px-2.5 py-1 rounded-full mb-4">
                {t("premium")}
              </span>
            )}
            <div className="w-12 h-12 rounded-xl bg-primary/15 text-primary flex items-center justify-center mb-5">{featured.icon}</div>
            <h3 className="text-[#25293C] font-bold text-2xl sm:text-3xl mb-3">{featured.title}</h3>
            <p className="text-[#5A5E6C] text-base leading-relaxed max-w-lg">{featured.description}</p>
          </div>
        </div>

        <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">
          {rest.map((f) => (
            <div key={f.title} className="group relative bg-[#F8F7FA] hover:bg-white border border-[#E8E7EA] hover:border-primary/20 rounded-2xl p-6 transition-all duration-200 hover:shadow-lg hover:shadow-primary/5">
              {f.premium && (
                <span className="absolute top-4 right-4 text-[10px] font-semibold text-primary bg-primary/10 px-2 py-0.5 rounded-full">
                  {t("premium")}
                </span>
              )}
              <div className="w-11 h-11 rounded-xl bg-primary/10 text-primary flex items-center justify-center mb-4 group-hover:bg-primary group-hover:text-white transition-all duration-200">{f.icon}</div>
              <h3 className="text-[#25293C] font-semibold text-lg mb-2">{f.title}</h3>
              <p className="text-[#8A8D93] text-sm leading-relaxed">{f.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Update `components/HowItWorks.tsx`**

```tsx
import { getTranslations } from "next-intl/server";

export default async function HowItWorks() {
  const t = await getTranslations("howItWorks");

  const steps = [
    { number: "01", emoji: "👤", title: t("step1Title"), description: t("step1Desc") },
    { number: "02", emoji: "🥗", title: t("step2Title"), description: t("step2Desc") },
    { number: "03", emoji: "📈", title: t("step3Title"), description: t("step3Desc") },
  ];

  return (
    <section className="bg-[#F8F7FA] py-24 px-5 sm:px-8">
      <div className="max-w-6xl mx-auto">
        <div className="max-w-xl mb-16">
          <p className="text-primary text-sm font-semibold uppercase tracking-widest mb-4">{t("eyebrow")}</p>
          <h2 className="text-4xl sm:text-5xl font-bold text-[#25293C] leading-tight">{t("headline")}</h2>
        </div>
        <div className="grid sm:grid-cols-3 gap-8">
          {steps.map((step, i) => (
            <div key={step.number} className="relative">
              {i < steps.length - 1 && (
                <div className="hidden sm:block absolute top-7 left-[calc(100%+1rem)] right-[-1rem] h-px bg-primary/25" />
              )}
              <div className="flex items-center gap-3 mb-5">
                <div className="w-12 h-12 rounded-full border-2 border-primary/30 bg-primary/10 flex items-center justify-center">
                  <span className="text-primary font-bold text-sm">{step.number}</span>
                </div>
                <span className="text-2xl">{step.emoji}</span>
              </div>
              <h3 className="text-[#25293C] font-semibold text-xl mb-3">{step.title}</h3>
              <p className="text-[#8A8D93] text-sm leading-relaxed">{step.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 4: Verify build**

```bash
npm run build
```

- [ ] **Step 5: Commit**

```bash
git add components/SocialProofBar.tsx components/Features.tsx components/HowItWorks.tsx
git commit -m "feat: translate SocialProofBar, Features, HowItWorks"
```

---

## Task 7: Translate PredictionTeaser and DishesPreview

**Files:**
- Modify: `components/PredictionTeaser.tsx`
- Modify: `components/DishesPreview.tsx`

- [ ] **Step 1: Update `components/PredictionTeaser.tsx`**

`PredictionTeaser` is a client component. Add `useTranslations`:

```tsx
// Add to imports:
import { useTranslations } from "next-intl";

// Inside PredictionTeaser(), add as first line:
const t = useTranslations("predictionTeaser");
```

Then replace hardcoded strings:

| Old | New |
|-----|-----|
| `"See your future"` | `{t("eyebrow")}` |
| `"See exactly how long it takes to reach your goal"` | `{t("headline")}` |
| `"Enter your current weight, goal weight..."` | `{t("subheadline")}` |
| `"Get my prediction"` | `{t("getPrediction")}` |
| `"View plans"` | `{t("viewPlans")}` |
| `"Your Prediction"` | `{t("cardEyebrow")}` |
| `<p className="text-2xl font-bold text-white/50 mb-6">days</p>` | `<p className="text-2xl font-bold text-white/50 mb-6">{t("days")}</p>` |
| `"to reach your goal weight — based on your profile"` | `{t("cardSubtext")}` |
| `{ label: "Current", value: "85 kg" }` | `{ label: t("current"), value: "85 kg" }` |
| `{ label: "Goal", value: "72 kg" }` | `{ label: t("goal"), value: "72 kg" }` |
| `{ label: "Weekly", value: "−0.5 kg" }` | `{ label: t("weekly"), value: "−0.5 kg" }` |
| `"Sample prediction · Sign up to see yours"` | `{t("sampleText")}` |

- [ ] **Step 2: Update `components/DishesPreview.tsx`**

Make async and add translations. Only the header section has translatable text:

```tsx
import Link from "next/link";
import DishCard from "./DishCard";
import { dishes } from "@/data/dishes";
import { getTranslations } from "next-intl/server";

export default async function DishesPreview() {
  const t = await getTranslations("dishesPreview");
  const row1 = [...dishes, ...dishes];
  const row2 = [...dishes.slice(Math.floor(dishes.length / 2)), ...dishes, ...dishes.slice(0, Math.floor(dishes.length / 2)), ...dishes];

  return (
    <section className="bg-white py-24 overflow-hidden">
      <style>{`
        @keyframes marquee { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }
        .marquee-row { display: flex; width: max-content; gap: 16px; }
        .marquee-row-1 { animation: marquee 40s linear infinite; }
        .marquee-row-2 { animation: marquee 28s linear infinite; }
      `}</style>
      <div className="px-5 sm:px-8 max-w-6xl mx-auto mb-12">
        <div className="flex flex-col sm:flex-row sm:items-end justify-between gap-6">
          <div className="max-w-xl">
            <p className="text-primary text-sm font-semibold uppercase tracking-widest mb-4">{t("eyebrow")}</p>
            <h2 className="text-4xl sm:text-5xl font-bold text-[#25293C] leading-tight">{t("headline")}</h2>
          </div>
          <Link href="/dishes" className="inline-flex items-center gap-2 text-primary hover:text-primary-dark font-semibold text-sm transition-colors shrink-0">
            {t("viewAll")}
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </Link>
        </div>
      </div>
      <div className="marquee-wrap flex flex-col gap-4">
        <div className="overflow-hidden">
          <div className="marquee-row marquee-row-1">
            {row1.map((dish, i) => (
              <div key={`r1-${dish.id}-${i}`} className="w-64 flex-shrink-0"><DishCard dish={dish} compact /></div>
            ))}
          </div>
        </div>
        <div className="overflow-hidden">
          <div className="marquee-row marquee-row-2">
            {row2.map((dish, i) => (
              <div key={`r2-${dish.id}-${i}`} className="w-64 flex-shrink-0"><DishCard dish={dish} compact /></div>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Verify build**

```bash
npm run build
```

- [ ] **Step 4: Commit**

```bash
git add components/PredictionTeaser.tsx components/DishesPreview.tsx
git commit -m "feat: translate PredictionTeaser and DishesPreview"
```

---

## Task 8: Translate PricingSection, CTASection, Footer

**Files:**
- Modify: `components/PricingSection.tsx`
- Modify: `components/CTASection.tsx`
- Modify: `components/Footer.tsx`

- [ ] **Step 1: Replace `components/PricingSection.tsx`**

```tsx
import Link from "next/link";
import CheckoutButton from "./CheckoutButton";
import { getTranslations } from "next-intl/server";

function Check({ color = "text-success" }: { color?: string }) {
  return (
    <svg width="16" height="16" viewBox="0 0 16 16" fill="none" className={`mt-0.5 flex-shrink-0 ${color}`}>
      <path d="M3 8l4 4 6-7" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
    </svg>
  );
}

export default async function PricingSection({ isLoggedIn = false }: { isLoggedIn?: boolean }) {
  const t = await getTranslations("pricing");

  const freeFeatures = [t("freeF1"), t("freeF2"), t("freeF3"), t("freeF4"), t("freeF5"), t("freeF6")];
  const premiumFeatures = [t("premiumF1"), t("premiumF2"), t("premiumF3"), t("premiumF4"), t("premiumF5"), t("premiumF6"), t("premiumF7"), t("premiumF8"), t("premiumF9"), t("premiumF10")];

  return (
    <section className="bg-[#F8F7FA] py-24 px-5 sm:px-8">
      <div className="max-w-6xl mx-auto">
        <div className="text-center max-w-xl mx-auto mb-14">
          <p className="text-primary text-sm font-semibold uppercase tracking-widest mb-4">{t("eyebrow")}</p>
          <h2 className="text-4xl sm:text-5xl font-bold text-[#25293C] leading-tight mb-4">{t("headline")}</h2>
          <p className="text-[#8A8D93] text-lg">{t("subheadline")}</p>
        </div>

        <div className="grid sm:grid-cols-2 gap-6 max-w-3xl mx-auto">
          <div className="bg-white border border-[#E8E7EA] rounded-2xl p-8 flex flex-col">
            <div className="mb-6">
              <h3 className="text-[#25293C] font-bold text-xl mb-1">{t("freeName")}</h3>
              <p className="text-[#8A8D93] text-sm">{t("freeTagline")}</p>
            </div>
            <div className="mb-8">
              <span className="text-4xl font-bold text-[#25293C]">$0</span>
              <span className="text-[#8A8D93] text-sm ml-2">{t("perMonth")}</span>
            </div>
            <ul className="space-y-3 mb-8 flex-1">
              {freeFeatures.map((f) => (
                <li key={f} className="flex items-start gap-3 text-sm text-[#8A8D93]">
                  <Check />
                  {f}
                </li>
              ))}
            </ul>
            {isLoggedIn ? (
              <Link href="/dashboard" className="w-full text-center py-3 rounded-xl font-semibold text-sm border border-[#E8E7EA] text-[#25293C] hover:bg-[#F8F7FA] transition-all duration-150">
                {t("freeCtaLoggedIn")}
              </Link>
            ) : (
              <Link href="/register" className="w-full text-center py-3 rounded-xl font-semibold text-sm border border-[#E8E7EA] text-[#25293C] hover:bg-[#F8F7FA] transition-all duration-150">
                {t("freeCta")}
              </Link>
            )}
          </div>

          <div className="relative bg-navy rounded-2xl p-8 flex flex-col overflow-hidden">
            <div className="absolute top-0 right-0 w-48 h-48 bg-primary/20 rounded-full blur-3xl pointer-events-none" />
            <div className="mb-6 relative">
              <h3 className="text-white font-bold text-xl mb-1">{t("premiumName")}</h3>
              <p className="text-white/50 text-sm">{t("premiumTagline")}</p>
            </div>
            <div className="mb-8 relative">
              <span className="text-4xl font-bold text-white">$15</span>
              <span className="text-white/40 text-sm ml-2">{t("perMonth")}</span>
              <p className="text-white/40 text-xs mt-1">{t("premiumCancelAnytime")}</p>
            </div>
            <ul className="space-y-3 mb-8 flex-1 relative">
              {premiumFeatures.map((f) => (
                <li key={f} className="flex items-start gap-3 text-sm text-white/70">
                  <Check color="text-primary" />
                  {f}
                </li>
              ))}
            </ul>
            <CheckoutButton className="relative w-full text-center py-3 rounded-xl font-semibold text-sm bg-primary hover:bg-primary-dark disabled:opacity-60 disabled:cursor-not-allowed text-white transition-all duration-150 shadow-xl shadow-primary/30">
              {t("premiumCta")}
            </CheckoutButton>
          </div>
        </div>

        <p className="text-center text-[#8A8D93] text-sm mt-8">{t("footer")}</p>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Replace `components/CTASection.tsx`**

```tsx
import Link from "next/link";
import { getTranslations } from "next-intl/server";

export default async function CTASection() {
  const t = await getTranslations("cta");

  const trustBadges = [
    { icon: (<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" strokeLinecap="round" strokeLinejoin="round"/></svg>), label: t("secureBilling") },
    { icon: (<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><polyline points="20 6 9 17 4 12" strokeLinecap="round" strokeLinejoin="round"/></svg>), label: t("cancelAnytime") },
    { icon: (<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2" strokeLinecap="round" strokeLinejoin="round"/></svg>), label: t("setupIn3min") },
  ];

  return (
    <section className="relative py-24 px-5 sm:px-8 overflow-hidden" style={{ background: "linear-gradient(135deg, #0d1a10 0%, #0f172a 50%, #0a1628 100%)" }}>
      <div className="absolute top-0 left-1/2 -translate-x-1/2 w-[500px] h-[200px] rounded-full blur-[100px] pointer-events-none" style={{ background: "rgba(74, 222, 128, 0.12)" }} />
      <div className="absolute bottom-0 left-1/4 w-64 h-64 rounded-full blur-[80px] pointer-events-none" style={{ background: "rgba(74, 222, 128, 0.07)" }} />
      <div className="absolute bottom-0 right-1/4 w-48 h-48 rounded-full blur-[80px] pointer-events-none" style={{ background: "rgba(99, 102, 241, 0.08)" }} />

      <div className="relative max-w-3xl mx-auto text-center">
        <p className="text-[#4ade80] text-sm font-semibold uppercase tracking-widest mb-4">{t("eyebrow")}</p>
        <h2 className="text-4xl sm:text-5xl lg:text-6xl font-bold text-white leading-tight mb-4">{t("headline")}</h2>
        <p className="text-white/40 text-base mb-2">
          {t("stat").replace(t("statHighlight"), "")}
          <span className="text-white/70 font-semibold">{t("statHighlight")}</span>
          {t("stat").split(t("statHighlight"))[1] ?? ""}
        </p>
        <p className="text-white/40 text-lg mb-10 max-w-xl mx-auto">{t("subheadline")}</p>
        <div className="flex flex-col sm:flex-row gap-4 justify-center mb-8">
          <Link href="/register" className="inline-flex items-center justify-center gap-2 bg-[#4ade80] hover:bg-[#22c55e] text-[#0d1a10] px-8 py-4 rounded-xl font-semibold text-base transition-colors shadow-xl shadow-[#4ade80]/25">
            {t("getStarted")}
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
            </svg>
          </Link>
          <Link href="/dishes" className="inline-flex items-center justify-center gap-2 text-white/60 hover:text-white px-8 py-4 rounded-xl font-semibold text-base transition-colors border border-white/10 hover:border-white/20">
            {t("browseMenu")}
          </Link>
        </div>
        <div className="flex flex-wrap items-center justify-center gap-5">
          {trustBadges.map(({ icon, label }) => (
            <div key={label} className="flex items-center gap-2 text-white/30">
              <span className="text-[#4ade80]">{icon}</span>
              <span className="text-sm">{label}</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Replace `components/Footer.tsx`**

```tsx
import Link from "next/link";
import { getTranslations } from "next-intl/server";

export default async function Footer() {
  const t = await getTranslations("footer");

  return (
    <footer className="bg-navy-deeper border-t border-white/[0.06] px-5 sm:px-8 py-14">
      <div className="max-w-6xl mx-auto">
        <div className="grid grid-cols-2 sm:grid-cols-4 gap-8 mb-12">
          <div className="col-span-2 sm:col-span-1">
            <Link href="/" className="flex items-center gap-2 mb-4">
              <div className="w-8 h-8 bg-primary rounded-lg flex items-center justify-center">
                <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                  <path d="M8 1.5C4.41 1.5 1.5 4.41 1.5 8S4.41 14.5 8 14.5 14.5 11.59 14.5 8 11.59 1.5 8 1.5ZM8 5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3Zm0 7.5a5.25 5.25 0 0 1-4.5-2.55c.02-1.5 3-2.325 4.5-2.325s4.48.825 4.5 2.325A5.25 5.25 0 0 1 8 12.5Z" fill="white" />
                </svg>
              </div>
              <span className="text-white font-semibold text-lg">Wondish</span>
            </Link>
            <p className="text-white/40 text-sm leading-relaxed">{t("tagline")}</p>
          </div>

          <div>
            <p className="text-white/60 text-xs font-semibold uppercase tracking-widest mb-4">{t("product")}</p>
            <ul className="space-y-3">
              {([["dishes", "/dishes"], ["pricing", "/pricing"], ["features", "/#features"]] as const).map(([key, href]) => (
                <li key={key}>
                  <Link href={href} className="text-white/40 hover:text-white/80 text-sm transition-colors">{t(key)}</Link>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <p className="text-white/60 text-xs font-semibold uppercase tracking-widest mb-4">{t("account")}</p>
            <ul className="space-y-3">
              <li><Link href="/login" className="text-white/40 hover:text-white/80 text-sm transition-colors">{t("login")}</Link></li>
              <li><Link href="/register" className="text-white/40 hover:text-white/80 text-sm transition-colors">{t("getStarted")}</Link></li>
            </ul>
          </div>

          <div>
            <p className="text-white/60 text-xs font-semibold uppercase tracking-widest mb-4">{t("legal")}</p>
            <ul className="space-y-3">
              <li><Link href="/privacy" className="text-white/40 hover:text-white/80 text-sm transition-colors">{t("privacyPolicy")}</Link></li>
              <li><Link href="/terms" className="text-white/40 hover:text-white/80 text-sm transition-colors">{t("terms")}</Link></li>
            </ul>
          </div>
        </div>

        <div className="border-t border-white/[0.06] pt-8 flex flex-col sm:flex-row items-center justify-between gap-4">
          <p className="text-white/30 text-sm">© {new Date().getFullYear()} Wondish. {t("rights")}</p>
          <p className="text-white/30 text-sm">{t("securedBy")} <span className="text-white/50 font-medium">Stripe</span></p>
        </div>
      </div>
    </footer>
  );
}
```

- [ ] **Step 4: Verify build**

```bash
npm run build
```

- [ ] **Step 5: Commit**

```bash
git add components/PricingSection.tsx components/CTASection.tsx components/Footer.tsx
git commit -m "feat: translate PricingSection, CTASection, Footer"
```

---

## Task 9: Translate forgot-password auth page

**Files:**
- Modify: `app/(auth)/forgot-password/page.tsx`

Note: `login` and `register` pages use Clerk's `<SignIn>` / `<SignUp>` hosted UI which handles its own translations internally. Only the custom-built auth pages need updating.

- [ ] **Step 1: Replace `app/(auth)/forgot-password/page.tsx`**

```tsx
"use client";

import { useState } from "react";
import Link from "next/link";
import Button from "@/components/ui/Button";
import { useTranslations } from "next-intl";

export default function ForgotPasswordPage() {
  const t = useTranslations("forgotPassword");
  const [email, setEmail] = useState("");
  const [loading, setLoading] = useState(false);
  const [sent, setSent] = useState(false);
  const [error, setError] = useState("");

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setLoading(true);
    setError("");
    try {
      const res = await fetch("/api/auth/forgot-password", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ email }),
      });
      if (!res.ok) {
        const data = await res.json();
        throw new Error(data.error ?? "Request failed");
      }
      setSent(true);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Unknown error");
    } finally {
      setLoading(false);
    }
  };

  if (sent) {
    return (
      <div className="w-full max-w-md">
        <div className="bg-navy-surface rounded-3xl p-8 border border-white/[0.06] text-center">
          <div className="w-16 h-16 bg-success/20 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <span className="text-3xl">📧</span>
          </div>
          <h1 className="text-white font-bold text-2xl mb-2">{t("successTitle")}</h1>
          <p className="text-white/50 text-sm mb-6">
            {t("successSubtitle", { email })}
          </p>
          <Link href="/login" className="text-primary text-sm font-medium hover:underline">
            {t("backToLogin")}
          </Link>
        </div>
      </div>
    );
  }

  return (
    <div className="w-full max-w-md">
      <div className="bg-navy-surface rounded-3xl p-8 border border-white/[0.06]">
        <h1 className="text-white font-bold text-2xl mb-1">{t("title")}</h1>
        <p className="text-white/50 text-sm mb-8">{t("subtitle")}</p>

        {error && (
          <div className="bg-error/10 border border-error/20 text-error rounded-xl px-4 py-3 text-sm mb-6">
            {error}
          </div>
        )}

        <form onSubmit={handleSubmit} className="space-y-5">
          <div>
            <label className="text-white/70 text-sm font-medium block mb-1.5">{t("emailLabel")}</label>
            <input
              type="email"
              required
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              className="w-full px-3.5 py-2.5 rounded-xl border border-white/10 bg-white/5 text-white text-sm placeholder:text-white/30 outline-none focus:border-primary focus:ring-2 focus:ring-primary/20 transition-all"
              placeholder={t("emailPlaceholder")}
            />
          </div>
          <Button type="submit" size="lg" loading={loading} className="w-full">
            {t("submit")}
          </Button>
        </form>

        <p className="text-center text-white/40 text-sm mt-6">
          {t("rememberedIt")}{" "}
          <Link href="/login" className="text-primary hover:underline font-medium">
            {t("signIn")}
          </Link>
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

- [ ] **Step 3: Commit**

```bash
git add "app/(auth)/forgot-password/page.tsx"
git commit -m "feat: translate forgot-password page"
```

---

## Task 10: Translate DashboardSidebar and DashboardHeader

**Files:**
- Modify: `components/dashboard/DashboardSidebar.tsx`
- Modify: `components/dashboard/DashboardHeader.tsx`

- [ ] **Step 1: Replace `components/dashboard/DashboardSidebar.tsx`**

```tsx
"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { useClerk } from "@clerk/nextjs";
import { useRouter } from "next/navigation";
import { useTranslations } from "next-intl";

export default function DashboardSidebar({ isAdmin }: { isAdmin: boolean }) {
  const pathname = usePathname();
  const { signOut } = useClerk();
  const router = useRouter();
  const t = useTranslations("sidebar");

  const navItems = [
    { href: "/overview", label: t("overview"), icon: "▦" },
    { href: "/meal-plan", label: t("mealPlan"), icon: "🍽" },
    { href: "/meal-plan/weekly", label: t("weeklyPlan"), icon: "📅" },
    { href: "/grocery-list", label: t("groceryList"), icon: "🛒" },
    { href: "/journal", label: t("myJournal"), icon: "📓" },
    { href: "/journey", label: t("myJourney"), icon: "📈" },
    { href: "/orders", label: t("myOrders"), icon: "📦" },
    { href: "/profile", label: t("myProfile"), icon: "👤" },
    { href: "/prediction", label: t("myPrediction"), icon: "🎯" },
    { href: "/taste", label: t("myTaste"), icon: "❤️" },
    { href: "/membership", label: t("myMembership"), icon: "⭐" },
  ];

  const adminItems = [
    { href: "/admin/recipes", label: t("recipes"), icon: "🧑‍🍳" },
    { href: "/admin/users", label: t("users"), icon: "👥" },
    { href: "/admin/companies", label: t("companies"), icon: "🏢" },
    { href: "/admin/parameters/gender", label: t("parameters"), icon: "⚙️" },
    { href: "/admin/coupons", label: t("coupons"), icon: "🎟️" },
  ];

  return (
    <aside className="fixed inset-y-0 left-0 z-30 w-64 bg-navy-dark flex flex-col border-r border-white/[0.06]">
      <div className="h-16 flex items-center px-6 border-b border-white/[0.06]">
        <a href="/" className="flex items-center gap-2.5">
          <div className="w-8 h-8 bg-primary rounded-lg flex items-center justify-center shadow-lg shadow-primary/30 flex-shrink-0">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M8 1.5C4.41 1.5 1.5 4.41 1.5 8S4.41 14.5 8 14.5 14.5 11.59 14.5 8 11.59 1.5 8 1.5ZM8 5a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3Zm0 7.5a5.25 5.25 0 0 1-4.5-2.55c.02-1.5 3-2.325 4.5-2.325s4.48.825 4.5 2.325A5.25 5.25 0 0 1 8 12.5Z" fill="white" />
            </svg>
          </div>
          <span className="text-white font-semibold text-lg">Wondish</span>
        </a>
      </div>

      <nav className="flex-1 overflow-y-auto py-4 px-3 scrollbar-hide">
        <div className="space-y-0.5">
          {navItems.map(({ href, label, icon }) => {
            const active = pathname === href || pathname.startsWith(href + "/");
            return (
              <Link key={href} href={href} className={`flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all ${active ? "bg-primary/20 text-primary" : "text-white/60 hover:text-white hover:bg-white/[0.06]"}`}>
                <span className="text-base w-5 text-center">{icon}</span>
                {label}
              </Link>
            );
          })}
        </div>

        {isAdmin && (
          <>
            <p className="text-white/30 text-xs font-semibold uppercase tracking-wider px-3 mt-6 mb-2">{t("admin")}</p>
            <div className="space-y-0.5">
              {adminItems.map(({ href, label, icon }) => {
                const active = pathname === href || pathname.startsWith(href + "/");
                return (
                  <Link key={href} href={href} className={`flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium transition-all ${active ? "bg-primary/20 text-primary" : "text-white/60 hover:text-white hover:bg-white/[0.06]"}`}>
                    <span className="text-base w-5 text-center">{icon}</span>
                    {label}
                  </Link>
                );
              })}
            </div>
          </>
        )}
      </nav>

      <div className="p-3 border-t border-white/[0.06]">
        <button
          onClick={() => signOut(() => router.push("/login"))}
          className="flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium text-white/50 hover:text-white hover:bg-white/[0.06] w-full transition-all"
        >
          <span className="text-base w-5 text-center">↩</span>
          {t("signOut")}
        </button>
      </div>
    </aside>
  );
}
```

- [ ] **Step 2: Replace `components/dashboard/DashboardHeader.tsx`**

Read the file first (already read above), then add `useTranslations("dashboardHeader")` and replace all hardcoded strings:

```tsx
"use client";

import { useEffect, useRef, useState } from "react";
import Link from "next/link";
import { useRouter, usePathname } from "next/navigation";
import { useClerk } from "@clerk/nextjs";
import { useTranslations } from "next-intl";

interface DashboardHeaderProps {
  email?: string | null;
  name?: string | null;
  plan: string;
  onMenuToggle?: () => void;
}

function CouponInput({ onClose }: { onClose: () => void }) {
  const t = useTranslations("dashboardHeader");
  const [code, setCode] = useState("");
  const [loading, setLoading] = useState(false);
  const [result, setResult] = useState<{ success: boolean; message: string } | null>(null);
  const router = useRouter();

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    if (!code.trim()) return;
    setLoading(true);
    setResult(null);
    try {
      const res = await fetch("/api/coupon/redeem", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ code }),
      });
      const data = await res.json();
      if (!res.ok) {
        setResult({ success: false, message: data.error });
      } else {
        setResult({ success: true, message: data.message });
        setCode("");
        setTimeout(() => { router.refresh(); onClose(); }, 1500);
      }
    } catch {
      setResult({ success: false, message: t("error") });
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="px-4 pb-4 pt-1">
      <p className="text-[#8A8D93] text-xs mb-3">{t("enterCoupon")}</p>
      <form onSubmit={handleSubmit} className="flex gap-2">
        <input
          autoFocus
          type="text"
          value={code}
          onChange={(e) => setCode(e.target.value.toUpperCase())}
          placeholder={t("enterCode")}
          disabled={loading}
          className="flex-1 min-w-0 bg-[#F8F7FA] border border-[#E8E7EA] rounded-lg px-3 py-2 text-xs font-mono text-[#25293C] placeholder:text-[#C0C0C4] uppercase tracking-widest focus:outline-none focus:border-primary/50"
        />
        <button
          type="submit"
          disabled={loading || !code.trim()}
          className="bg-primary hover:bg-primary-dark text-white px-3 py-2 rounded-lg text-xs font-semibold transition-colors disabled:opacity-50"
        >
          {loading ? "…" : t("apply")}
        </button>
      </form>
      {result && (
        <p className={`mt-2 text-xs font-medium ${result.success ? "text-emerald-600" : "text-red-500"}`}>
          {result.success ? "✓" : "✗"} {result.message}
        </p>
      )}
    </div>
  );
}

export default function DashboardHeader({ email, name, plan, onMenuToggle }: DashboardHeaderProps) {
  const t = useTranslations("dashboardHeader");
  const [open, setOpen] = useState(false);
  const [showCoupon, setShowCoupon] = useState(false);
  const dropdownRef = useRef<HTMLDivElement>(null);
  const { signOut } = useClerk();
  const router = useRouter();

  useEffect(() => {
    function handler(e: MouseEvent) {
      if (dropdownRef.current && !dropdownRef.current.contains(e.target as Node)) {
        setOpen(false);
        setShowCoupon(false);
      }
    }
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, []);

  return (
    <header className="h-16 bg-white border-b border-[#E8E7EA] flex items-center justify-between px-5 sm:px-8">
      <button className="lg:hidden flex flex-col gap-1.5 p-1" onClick={onMenuToggle} aria-label="Toggle menu">
        <span className="w-5 h-0.5 bg-[#25293C] block" />
        <span className="w-5 h-0.5 bg-[#25293C] block" />
        <span className="w-5 h-0.5 bg-[#25293C] block" />
      </button>

      <div className="flex-1" />

      <div className="flex items-center gap-3">
        <span className="text-[#8A8D93] text-sm hidden sm:block">{email}</span>
        {plan === "ADMIN" ? (
          <Link href="/membership" className="text-xs font-bold px-2.5 py-1 rounded-full bg-amber-100 text-amber-700 hover:bg-amber-200 transition-colors">Admin ✦</Link>
        ) : plan === "PREMIUM" ? (
          <Link href="/membership" className="text-xs font-bold px-2.5 py-1 rounded-full bg-primary/10 text-primary hover:bg-primary/20 transition-colors">Premium ✦</Link>
        ) : (
          <Link href="/pricing" className="text-xs font-bold px-2.5 py-1 rounded-full bg-[#F3F2FF] text-[#8A8D93] hover:bg-primary/10 hover:text-primary transition-colors">
            {t("upgrade")}
          </Link>
        )}

        <div ref={dropdownRef} className="relative">
          <button
            onClick={() => { setOpen((v) => !v); setShowCoupon(false); }}
            className="w-8 h-8 rounded-full bg-primary/20 flex items-center justify-center text-primary font-semibold text-sm hover:bg-primary/30 transition-colors"
          >
            {name?.charAt(0).toUpperCase() ?? "?"}
          </button>

          {open && (
            <div className="absolute right-0 top-10 w-56 bg-white border border-[#E8E7EA] rounded-2xl shadow-xl shadow-black/8 z-50 overflow-hidden">
              <div className="px-4 py-3 border-b border-[#F0F0F2]">
                <p className="text-[#25293C] font-semibold text-sm truncate">{name}</p>
                <p className="text-[#8A8D93] text-xs truncate">{email}</p>
              </div>

              {!showCoupon ? (
                <>
                  <div className="py-1.5">
                    <Link href="/profile" onClick={() => setOpen(false)} className="flex items-center gap-3 px-4 py-2 text-sm text-[#4A4E5C] hover:bg-[#F8F7FA] transition-colors">
                      <span className="text-base">👤</span> {t("myProfile")}
                    </Link>
                    <button onClick={() => setShowCoupon(true)} className="w-full flex items-center gap-3 px-4 py-2 text-sm text-[#4A4E5C] hover:bg-[#F8F7FA] transition-colors text-left">
                      <span className="text-base">🎟️</span> {t("redeemCoupon")}
                    </button>
                  </div>
                  <div className="border-t border-[#F0F0F2] py-1.5">
                    <button onClick={() => signOut(() => router.push("/login"))} className="w-full flex items-center gap-3 px-4 py-2 text-sm text-red-500 hover:bg-red-50 transition-colors text-left">
                      <span className="text-base">↩</span> {t("signOut")}
                    </button>
                  </div>
                </>
              ) : (
                <>
                  <div className="px-4 pt-3 pb-1 flex items-center gap-2">
                    <button onClick={() => setShowCoupon(false)} className="text-[#8A8D93] hover:text-[#25293C] transition-colors">←</button>
                    <p className="text-[#25293C] font-semibold text-sm">{t("redeemCouponTitle")}</p>
                  </div>
                  <CouponInput onClose={() => { setOpen(false); setShowCoupon(false); }} />
                </>
              )}
            </div>
          )}
        </div>
      </div>
    </header>
  );
}
```

- [ ] **Step 3: Verify build**

```bash
npm run build
```

- [ ] **Step 4: Commit**

```bash
git add components/dashboard/DashboardSidebar.tsx components/dashboard/DashboardHeader.tsx
git commit -m "feat: translate DashboardSidebar and DashboardHeader"
```

---

## Task 11: Translate the Overview dashboard page

**Files:**
- Modify: `app/(dashboard)/overview/page.tsx`

- [ ] **Step 1: Add translations to `app/(dashboard)/overview/page.tsx`**

Add this import at the top:
```tsx
import { getTranslations } from "next-intl/server";
```

At the start of the `OverviewPage` function body, after all the data fetching, add:
```tsx
const t = await getTranslations("overview");
```

Then replace the JSX strings:

| Old | New |
|-----|-----|
| `` `Good to see you, ${account.firstName} 👋` `` | `{t("greeting", { name: account.firstName })}` |
| `"Here's your health snapshot for today."` | `{t("snapshot")}` |
| `"Today's Meals"` | `{t("todaysMeals")}` |
| `"meals completed"` | `{t("mealsCompleted")}` |
| `"Current Weight"` | `{t("currentWeight")}` |
| `"from last journal entry"` | `{t("fromLastJournal")}` |
| `"Streak"` | `{t("streakLabel")}` |
| `` `${streak} days` `` | `{t("streakDays", { count: streak })}` |
| `"consecutive journal entries"` | `{t("consecutiveEntries")}` |
| `"Today's Meal Plan"` | `{t("todaysMealPlan")}` |
| `"View all →"` | `{t("viewAll")}` |
| `"No meal plan for today."` | `{t("noMealPlan")}` |
| `"Set up meal plan"` | `{t("setUpMealPlan")}` |
| `"Unlock the full experience"` | `{t("upgradeTitle")}` |
| `"Upgrade to Premium for custom ingredients..."` | `{t("upgradeDesc")}` |
| `"Upgrade to Premium — $15/mo"` | `{t("upgradeCta")}` |

- [ ] **Step 2: Verify build**

```bash
npm run build
```

- [ ] **Step 3: Commit**

```bash
git add "app/(dashboard)/overview/page.tsx"
git commit -m "feat: translate overview dashboard page"
```

---

## Task 12: Remove Testimonials from the marketing page

**Files:**
- Modify: `app/(main)/page.tsx`

- [ ] **Step 1: Remove `<Testimonials />` from `app/(main)/page.tsx`**

Replace the file with:

```tsx
import Hero from "@/components/Hero";
import SocialProofBar from "@/components/SocialProofBar";
import Features from "@/components/Features";
import PredictionTeaser from "@/components/PredictionTeaser";
import HowItWorks from "@/components/HowItWorks";
import DishesPreview from "@/components/DishesPreview";
import PricingSection from "@/components/PricingSection";
import CTASection from "@/components/CTASection";

export default function HomePage() {
  return (
    <>
      <Hero />
      <SocialProofBar />
      <Features />
      <PredictionTeaser />
      <HowItWorks />
      <DishesPreview />
      <PricingSection />
      <CTASection />
    </>
  );
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

- [ ] **Step 3: Commit**

```bash
git add "app/(main)/page.tsx"
git commit -m "feat: remove Testimonials section from marketing page"
```

---

## Self-Review Notes

- All translation keys used in components match the keys defined in the message files exactly (e.g. `streakLabel`, not `streak`)
- `DashboardHeader`'s `CouponInput` sub-component also uses `useTranslations` — it gets its own `t` call
- `CTASection` stat text uses `t("stat")` and `t("statHighlight")` with a string split to preserve the bold highlight — if this feels fragile, simplify by splitting the stat sentence into `statPrefix` + `statHighlight` + `statSuffix` keys in the JSON
- `next-intl` interpolation for `{name}` and `{count}` uses `t("key", { name: value })` syntax — this is the standard next-intl v3 pattern
- The `forgotPassword.successSubtitle` key uses `{email}` interpolation: `t("successSubtitle", { email })`
- Login/register pages (Clerk hosted UI) are intentionally not translated here — Clerk has its own localization system separate from next-intl
- `DashboardHeader` references `t("error")` for the catch block — add `"error": "Something went wrong."` to `dashboardHeader` in all three message files, or use `t` from `common` namespace instead. Simplest fix: change the catch block to use a hardcoded fallback string and skip the `t("error")` call in that component
